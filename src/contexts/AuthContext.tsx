
// Redirect file for backward compatibility
import { AuthProvider, useAuth, AuthContext } from './auth/AuthContext';
import type { User, AuthContextType } from './auth/AuthContext';

export { AuthContext, AuthProvider, useAuth };
export type { User, AuthContextType };
