import React from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

const GridManagement: React.FC = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Grid Management</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Grid management controls and monitoring will be displayed here.</p>
      </CardContent>
    </Card>
  );
};

export default GridManagement; 